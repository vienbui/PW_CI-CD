# Single URL (all envs use this if set)
# API_URL=https://api.example.com

# Or: set ENV and use per-env URLs (when running ENV=ag1, ENV=stg, ENV=prod)
ENV=stg
API_URL_AG1=https://api-ag1.example.com
API_URL_STG=https://api-stg.example.com
API_URL_PROD=https://api.example.com
